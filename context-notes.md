# 한국 주식 자동매매 시스템 — 컨텍스트 노트

---

## 최근 세션 요약 (2026-06-09 마지막 업데이트)

### 현재 시스템 상태
- 모의투자 운영 중 (2026-05-12 시작)
- 누적 실현손익: +1,326,958원, 거래 18건
- EC2 서비스 정상 실행 중 (`systemd stock-bot.service`)
- 코드 브랜치: `claude/sleepy-heisenberg-8hximg`

### 오늘 완료한 작업 (2026-06-09)
1. **KIS_BASE_URL 포트 수정** — 모의투자 조회 API(`inquire-price` 등)가 `9443` 포트에서 500 오류 반환. 원인은 모의투자 서버(openapivts)의 조회/주문 API 모두 `29443` 포트 사용. `config/settings.py`의 `KIS_BASE_URL`을 `9443` → `29443`으로 수정. `real_trading_transition.md` 섹션 5의 포트 표가 구버전이었음 (조회도 29443 사용으로 확인됨).
2. **데이터 수집 전체 점검** — 모든 CSV 파일 정상 수집 중 확인. basis_log 공백 없음 (첫/마지막 행만 보고 착각).

### 현재 시장 환경 (2026-06-09 기준)
- 어제 및 지난주말 폭락장 발생
- VKOSPI **76.63** (극도 고변동성, 정상 10~35)
- 베이시스 **-13.5pt (-1.04%)** (심각한 백워데이션, 임계값 -0.3% 대비 3배 초과)
- 최근 거래 연속 손절 중 — 폭락장 데이터 수집 진행 중

### 데이터 수집 현황 (2026-06-09 기준)
| 파일 | 현황 | 비고 |
|------|------|------|
| trades.csv | 18건 | 목표 30건까지 약 2~3주 |
| signal_log.csv | 338건 | 정상 |
| basis_log.csv | 13거래일 (05-19~06-05) | 정상 연속 수집 중 |
| timing_log.csv | 257건 | 정상 |
| followup_pending.json | 17건 추적 중 | d20 완료 시 followup_log.csv 생성 |
| followup_log.csv | 미생성 | 정상 — d20 완료 종목 없음 (6/16 첫 완료 예정) |

### EC2 환경 주요 설정
- venv 경로: `/home/ubuntu/stock-bot/venv/bin/python3`
- 서비스 파일: `/etc/systemd/system/stock-bot.service`
- override: `/etc/systemd/system/stock-bot.service.d/override.conf` (KRX_API_KEY 등록)
- 코드 경로: `/home/ubuntu/stock-bot/`

### 다음 세션 할 일
- 특별한 이슈 없으면 매일 `/check` 결과로 정상 여부만 확인
- **거래 30건 도달 시** → `analysis_plan.md` 기준으로 파라미터 검증 시작
- **6/16** → followup d20 첫 완료 (5/18 레이저쎌 매도분)
- **basis_log 40일 도달 시 (약 7월 초)** → 베이시스/slope 임계값 결정 후 `morning_routine()` 필터 추가
- **basis_log 60일 도달 시 (약 8월 중순)** → VKOSPI MA60 필터 결정 후 추가
- 데이터 충분 후 `real_trading_transition.md` 미완료 항목 순서대로 실전 전환 준비

### 방침
- 모의투자 중이므로 **시스템 수정 없이 데이터 축적 우선**
- 폭락장 포함 다양한 시장 상황 데이터가 쌓여야 파라미터 튜닝 의미 있음
- 베이시스/VKOSPI 필터는 데이터 기반으로 임계값 결정 후 추가 예정

---

## 프로젝트 개요

코스피 대형주 위주로 네이버 증권 테마 크롤링 + 외국인 매매 동향을 참고하여 여러 종목을 자동으로 매매하는 시스템.

---

## 확정 항목

| 요소 | 결정값 |
|------|--------|
| 운용 규모 | 1,000만원 (실현 손익 누적 복리 재투자) |
| 선정 종목 수 | 최대 4종목 (테마 집중 허용) |
| 긴급 손절 | 진입가 대비 -8% → 즉시 매도 (캔들 무시) |
| 손절 | 진입가 대비 -5% + 직전 1분봉 종가 확인 |
| 본전 보호 | +7% 달성 시 스탑을 진입가 +2%로 이동 |
| 모멘텀 약화 익절 | +10% 이상 + 1분봉 하락 전환 시 즉시 매도 |
| 부분 익절 | +20% 달성 시 50% 매도, 잔량 트레일링 |
| 트레일링 스탑 | 고점 대비 -12% + 직전 1분봉 종가 확인 |
| 시간 손절 | 20거래일 경과 + 수익률 +5% 미만 시 강제 청산 |
| API | 한국투자증권 KIS API |
| 언어 | Python |
| 실행 환경 | AWS EC2 t3.micro (서울 리전, 1년 무료) |
| 모니터링 | 텔레그램 봇 (알림 + 수동매매 + 시스템 제어) |

---

## 핵심 전략

### 매수 4단계 필터

| 단계 | 조건 | 비고 |
|------|------|------|
| 0단계 | KOSPI MA60 시장 추세 필터 | KODEX 200(069500) 종가 > MA60 이면 매수 허용 |
| 1단계 | 네이버 테마 크롤링 | 당일 등락률 상위 12개 테마 수집 → 모멘텀 상위 3개 선정 |
| 2단계 | 현재가 > MA20 > MA60 + 전일 거래량 필터 | 상승추세 + 거래량 확인 |
| 3단계 | 외국인 5일 누적 순매수 > 0 | 외국인 동향 필터 |

- 전체 통과 종목 중 모멘텀 상위 4종목 선정 (테마별 제한 없음)
- 09:15~09:30 눌림목 진입 (1분봉 기반, 09:30 강제 시장가)

### 매도 7단계 로직 (1분봉 기반)

| 단계 | 조건 | 행동 |
|------|------|------|
| 긴급 손절 | 현재가 ≤ 진입가 × 0.92 | 즉시 전량 매도 (캔들 무시) |
| 손절 | 직전 1분봉 종가 ≤ 진입가 × 0.95 | 전량 매도 (whipsaw 방지) |
| 본전 보호 | +7% 달성 시 1회 발동 | 스탑을 진입가 +2%로 이동 |
| 모멘텀 약화 | +10% 이상 + 현재가 < 직전봉 저가 + 현재가 < 5분 MA | 즉시 전량 매도 |
| 부분 익절 | +20% 달성 시 1회 발동 | 보유량 50% 매도, 잔량 트레일링 재개 |
| 트레일링 스탑 | 직전 1분봉 종가 ≤ 고점 × 0.88 | 잔량 전량 매도 |
| 시간 손절 | 20거래일 경과 + 수익 <+5% | 잔량 전량 강제 청산 |

### 스케줄

- 08:00 — 네이버 테마 크롤링 + 신호 분석 시작
- 09:15~09:30 — 1분마다 눌림목 조건 체크 후 매수 (미충족 시 09:30 강제 진입)
- 09:00~15:30 — 2분마다 1분봉 기반 매도 조건 모니터링
- 15:35 — 일일 결산 리포트 + 사후 추적 가격 기록
- 주말/공휴일 — 자동 스킵

---

## 테마 수집 방식

### 동적 크롤링 (naver_theme.py)

매일 08:00에 네이버 증권 테마 페이지를 크롤링하여 당일 등락률 상위 12개 테마를 자동 수집. 시장 주도 테마가 바뀌어도 자동으로 반영됨.

**3단계 방어 구조.**

| 단계 | 방법 | 발동 조건 |
|------|------|-----------|
| 1단계 | 네이버 크롤링 (3회 재시도) | 정상 |
| 2단계 | 어제 캐시 (`theme_cache.json`) | 크롤링 실패 시 |
| 3단계 | 하드코딩 기본 10개 테마 | 캐시도 없을 때 |

2, 3단계 발동 시 텔레그램으로 알림 전송.

**종목 필터링 기준.**
- 시총 5,000억 이상
- 종목코드 6자리 (KOSPI 기준)
- 테마당 최대 5종목 수집

### 하드코딩 폴백 테마 (10개)

반도체, 2차전지, 바이오/제약, 방산, 자동차, 금융, 조선/중공업, AI/플랫폼, 철강/소재, 에너지/화학

---

## 에러 모니터링 (error_monitor.py)

| 상황 | 동작 |
|------|------|
| 일반 에러 (현재가 조회 실패 등) | error.log 기록 + 30분에 1회 텔레그램 알림 |
| 심각 에러 (매수/매도 실패, KIS API 오류) | 즉시 텔레그램 알림 (쿨다운 무시) |
| 스케줄러 30분 이상 무응답 | 워치독 스레드가 즉시 텔레그램 알림 |

- RotatingFileHandler: 1MB × 5파일 회전 보관
- `/errors [n]` 명령어로 텔레그램에서 즉시 조회

---

## 기술 스택

| 항목 | 내용 |
|------|------|
| 언어 | Python 3.12 |
| 서버 | AWS EC2 t3.micro, Ubuntu 24.04, 서울 리전 |
| API | KIS API (REST, 모의투자) |
| 데이터 | KIS 일봉, 투자자별 매매동향, 네이버 증권 테마 크롤링 |
| 알림 | Telegram Bot API |
| 프로세스 관리 | systemd (자동 시작 + 재시작) |
| 코드 저장 | GitHub (claude/file-modifications-main-ykk5n 브랜치) |

---

## 주요 파일 구조

```
~/stock-bot/
├── main.py              # 메인 루프 (스케줄러 + 텔레그램 봇)
├── kis_auth.py          # KIS API 인증 토큰
├── kis_data.py          # 현재가 + 1분봉 조회
├── kis_indicator.py     # 일봉 + MA20/MA60 + KOSPI 추세 필터
├── kis_momentum.py      # 모멘텀 스코어링
├── naver_theme.py       # 네이버 테마 크롤링 + 3단계 방어
├── kis_sector.py        # 동적 테마 기반 주도주 선정
├── kis_foreign.py       # 외국인 5일 순매수 필터
├── kis_order.py         # 매수/매도 주문 (500 오류 5회 재시도)
├── kis_balance.py       # 잔고 조회 (실전 전환 후 사용)
├── basis_collector.py   # KOSPI 200 베이시스 수집 (네이버 스크래핑)
├── telegram_bot.py      # 텔레그램 명령어 처리
├── performance.py       # 거래 기록 및 성과 평가
├── error_monitor.py     # 에러 로깅 + 텔레그램 알림
├── trades.csv           # 매도 시 자동 기록 (깃허브 미포함)
├── signal_log.csv       # 매일 스캔 결과 전체 기록 (깃허브 미포함)
├── basis_log.csv        # 매일 베이시스 기록 — 임계값 튜닝용 (깃허브 미포함)
├── timing_log.csv       # 매수 윈도우 분별 체크 기록 — 진입 최적화용 (깃허브 미포함)
├── followup_log.csv     # 매도 후 3/5/10/20일 사후 추적 (깃허브 미포함)
├── error.log            # 에러 로그 파일 (깃허브 미포함)
├── config/
│   ├── settings.py           # 환경변수 및 전역 설정
│   ├── token_cache.json      # KIS 토큰 캐시
│   ├── theme_cache.json      # 네이버 테마 캐시 (크롤링 실패 시 사용)
│   └── followup_pending.json # 사후 추적 대기 목록
└── .env                 # 키값 (깃허브 미포함)
```

---

## 주요 결정 사항

- **1분봉 기반 매수/매도**: 눌림목 진입(매수)과 캔들 종가 확인(매도)으로 고점 추격 매수와 whipsaw 손절을 동시에 방지. 매수는 09:15~09:30 1분 루프, 매도는 2분 루프.
- **긴급 손절 추가**: -8% 이상 급락 시 캔들 확인 없이 즉시 매도. 일반 손절(-5%)은 1분봉 종가 확인 후 매도.
- **모멘텀 약화 조기 익절**: +10% 이상 수익 구간에서 1분봉 하락 전환 시 트레일링보다 먼저 빠져나옴. 고점 부근 탈출 목적.
- **단계별 매도 로직**: 기존 손절+트레일링에서 긴급손절·모멘텀약화·본전보호·부분익절·시간손절 7단계로 확장.
- **트레일링 스탑 완화**: 10% → 12%. 부분 익절 도입으로 이익이 일부 확보되므로 잔량에 더 많은 여유를 줌.
- **복리 재투자**: realized_pnl을 누적해서 다음 매수 기준금 자동 조정. get_available_cash() = 총자산 - 보유 포지션 투자금.
- **종목 선정 방식**: 테마당 2종목 고정 → 전체 통과 종목 중 모멘텀 상위 4종목으로 변경. 특정 테마에 집중될 수 있으나 모멘텀 기반이므로 수익성 우선.
- **외국인 순매수 임계값**: 현재 0원 (순매수면 통과). 모의투자 결과 후 상향 조정 예정.
- **네이버 테마 크롤링**: KIS API가 테마 분류를 미지원(2021년 이후 관리 중단)하여 네이버 증권 크롤링으로 대체. 3단계 방어로 안정성 확보.
- **KIS 잔고조회 API**: 모의투자 서버 미지원. 실전 전환 시 활성화.
- **외국인 데이터**: KIS 투자자별 매매동향 API 사용 (프로그램 매매 전용 아닌 전체 외국인 매매).
- **토큰 캐시**: KIS 모의투자 토큰 하루 1회 발급 제한으로 파일 캐시 적용.
- **에러 모니터링**: 기존 except: pass 구간 전체 교체. 파일 로그 + 심각 에러 텔레그램 즉시 알림으로 운영 중 문제 파악 가능.
- **KIS 모의서버 500 오류 대응**: 매수 주문 5회 재시도(3초 간격). 실패 시 종목을 대기열에 재등록해 1분 후 재시도. 09:30 강제매수도 3회 재시도.
- **매수 시작 시간 09:15**: KIS 모의투자 서버가 09:10~09:14 구간에 집중적으로 500 오류를 반환하는 패턴 확인 후 09:15로 조정.
- **워치독 오경보 수정**: morning_routine이 스케줄러 스레드를 블로킹하는 동안 heartbeat 미갱신 문제 수정. 09:15 대기 후 및 매 루프마다 갱신.
- **CSV 데이터 확장**: 파라미터 튜닝 분석을 위해 trades.csv에 entry_time, exit_time, peak_price, min_price, trigger_price, momentum, foreign_net_buy_mil, ma20/ma60_at_entry, volume_ratio, kospi_trend, dip_entry_used 추가. signal_log.csv에 signal_price, ma20, ma60, volume_ratio 추가.
- **사후 추적 (followup_log.csv)**: 매도 후 3/5/10/20일 가격을 자동 기록. 손절이 너무 빨랐는지, 트레일링이 적절했는지 분석용.
- **베이시스 수집 (basis_log.csv)**: 매일 09:15 KOSPI 200 베이시스(선물-현물)를 네이버에서 스크래핑 후 기록. 매매 조건으로는 미사용, 데이터 축적 후 임계값 결정 예정.
- **타이밍 로그 (timing_log.csv)**: 매수 윈도우(09:15~09:30) 매 분 체크 결과 기록. 눌림목 조건 효과 및 최적 진입 시간 분석용.
- **signal_log 헤더 불일치 수정 (2026-05-26)**: signal_price 컬럼이 나중에 추가되면서 기존 CSV 헤더가 구버전(11열)으로 남아 있었음. DictReader가 위치 기반으로 매핑하므로 passed_all_filters가 ma20 값을 읽어 필터통과 0, 선정 0으로 오보. log_signal_scan()에 헤더 불일치 자동 감지/교체 로직 추가.
- **VKOSPI 수집 방식 확정 (2026-05-26)**: Naver polling API(장 중 포함 datas=[] 반환), pykrx 1.2.8(KRX 로그인 필수로 변경), FinanceDataReader(Yahoo Finance 미지원), fchart/sise_index(JS 렌더링) 모두 미지원 확인. KRX OpenAPI(data-dbg.krx.co.kr) 파생상품지수 시세정보 API로 해결. AUTH_KEY 헤더 + JSON POST 방식. '코스피 200 변동성지수' 항목 종가 추출. KRX_API_KEY 환경변수를 systemd override.conf에 등록.
- **워치독 타임아웃 90분 (2026-05-26 이전)**: 5테마 스캔이 30분+ 소요되어 morning_routine 블로킹 중 heartbeat 미갱신 → 오경보 발생. WATCHDOG_TIMEOUT 1800 → 5400(90분)으로 수정.
- **백테스팅 한계**: 네이버 테마 과거 랭킹 데이터가 없어 완전한 백테스트 불가. 가격 기반 필터(BB%B, MA, ATR)만 부분 백테스트 가능. 핵심 알파인 테마 선정 로직은 실제 모의투자 데이터로만 검증 가능.

---

## 성과 평가 기준

모의투자 결과를 아래 기준으로 판단.

| 지표 | 최소 기준 | 확인 방법 |
|------|-----------|-----------|
| 승률 | 45% 이상 | `/report` |
| 손익비 | 1.5 이상 | `/report` |
| MDD | -20% 이내 | `/report` |
| KOSPI 초과수익 | 플러스 | 수동 비교 |
| 거래 건수 | 30건 이상 | 통계 신뢰도 확보 후 판단 |

평가 일정.

- 1주차: 시스템 안정성 확인 (오류 없이 작동하는지, error.log 점검)
- 1개월: 승률/손익비 1차 확인
- 3개월: 전략 유효성 판단 및 파라미터 튜닝
- 6개월 이상: 실전 전환 여부 결정

---

## 다음 단계

1. 서버에 최신 코드 반영 (reference.md 배포 명령어 참고)
2. 모의투자로 실전 검증 (매수 신호 → 주문 → 단계별 매도 전 과정 확인)
3. error.log 및 `/errors` 로 안정성 모니터링
4. `/report` 로 누적 성과 주기적 확인
5. 30건 이상 거래 후 CSV 데이터 분석 → 파라미터 튜닝 (외국인 임계값, 매수 시간, 베이시스 임계값)
6. 실전 투자 전환 (checklist.md Phase 7 참고)

---

## 실전 투자 전환 시 주요 변경 사항

| 항목 | 현재 (모의) | 실전 전환 후 |
|------|------------|-------------|
| KIS_IS_MOCK | true | false |
| 앱키/시크릿 | 모의투자용 | 실전용 별도 발급 |
| 계좌번호 | 모의투자 계좌 | 실전 계좌 |
| 가용현금 계산 | settings.py 고정값 | kis_balance.py 실제 잔고 조회 |
| SSL 검증 | 비활성화 | 자동 활성화 (코드 수정 불필요) |
| 수수료/세금 | 미반영 | 증권거래세 0.18% + 수수료 반영 필요 |
| 토큰 캐시 | 모의투자 토큰 | 삭제 후 실전 토큰 새로 발급 |

**전환 전 반드시 확인.**
- 모의투자 30건 이상, 승률 45%+, 손익비 1.5+ 달성 후 전환
- 전환 당일 소액 테스트 주문으로 정상 체결 확인
- 자세한 절차는 checklist.md Phase 7 참고

### 2026-06-25 — 장중 코스피 급락 가드 추가

**문제.** 모의투자 29건 분석 결과, 특정 진입일(06-19 3건, 06-05·06-03 2건 등)에 그날 진입분이 통째로 전손. 원인은 시장 필터가 `check_market_trend()`의 일봉 MA60(전일 종가) 하나뿐이라 "장중 급락일"을 못 잡음. 진입 19건 중 15건이 09:30 이전이라 지수 방향 확인 전 일찍 들어가 동반 손절.

**결정.** 별도 종가 반등매매(평균회귀)는 검증 데이터가 없어 보류. 대신 아침 급락 가드만 추가 — 급락일 매수 보류 → 현금 보존 → 익일 모멘텀 루틴이 반등 주도주를 자연 참여(기존 로직 재사용, 최소 변경).

**구현.** `MARKET_CRASH_GUARD_RATE`(.env, 기본 1.0%, 0이면 비활성). `kis_indicator.get_market_intraday_change()`가 KODEX200(069500) 당일 등락률 반환(실패 시 0.0 폴백). `morning_routine` 진입 루프(09:15~10:00) 매 사이클 점검, 등락률 ≤ -임계값이면 그날 신규 매수 전면 보류·break. 09:40 발생 급락도 포착.

**검증 한계.** 우리 데이터엔 장중 코스피 시계열이 없어 임계값 정밀 보정 불가. -1.0%는 보수적 기본값. 배포 후 급락일에 가드 발동 로그(skipped_market_crash)로 적정성 재검토 필요.

### 2026-06-25 (추가) — 급락 가드 임계값 -1.5% + 지수 등락률 로깅

**임계값 -1.0% → -1.5%.** basis_log의 kospi200_spot 전일대비 변화를 검토했으나 std 4.75%(일변동 -7~+12%)로 비현실적 — 베이시스 수집기의 다른 소스라 노이즈 큼, 가드가 보는 KODEX200 change_rate와 무관. 손절군집 5일 중 3일(06-24·06-22·06-05)만 실제 지수 하락, 06-03·06-19는 지수 멀쩡(종목 고유 손실)이라 가드 무관. 가드가 09:15~10:00 장중 '터치' 기준이라 -1%는 회복할 날도 거를 만큼 민감 → 진짜 급락만 거르도록 -1.5%로 완화. 수익이 소수 모멘텀 승자(+15~22%)에 집중돼 과필터 비용이 큰 점도 고려.

**로깅.** 정밀 보정 데이터가 없어서 추측이므로, log_market()으로 매일 아침 KODEX200 당일 등락률 1행을 market_log.csv에 축적. 2~3주 뒤 "발동 빈도 vs 그날 실제 시장"으로 데이터 기반 재보정 예정. (우리 원칙: 검증 후 튜닝)

### 2026-06-25 (추가2) — 가드 분당 throttle + 지수 궤적 로깅 (기울기 보정 대비)

**사용자 제안: 장초반 분단위 지수 하락 '기울기'로 급락장 판단.** 평가 — 타당한 기법(지수 장중 ROC/속도)이나 활성 가드로는 시기상조. ① 파라미터 2개(측정구간·기울기임계)로 레벨(1개)보다 많음 ② 분봉 시계열 검증데이터 0 → 블라인드 튜닝=과적합 ③ 개장직후 기울기 노이즈 최대 ④ 레벨-터치 가드가 이미 '빠름'을 시간-도달로 거칠게 반영. 결론: 레벨 -1.5% 유지, 기울기는 데이터 모아 비교 후 v2 검토.

**구현.** 가드를 진입 루프 매 사이클 호출 → 분당 1회 throttle(`last_idx_minute`). 부수효과로 (a) 개장 변동성 1분 평활 (b) 아침 API 호출 수백→~15회 (c) 분당 지수 궤적이 market_log.csv에 쌓여 나중에 기울기 계산 가능. log_market은 가드 on/off 무관하게 매 분 기록(rate=0이어도 데이터 수집). 시뮬레이션 테스트로 throttle·발동·미발동 검증.

**미결.** 진입 시작 09:15(코드) vs 09:20(주석 의도) 불일치 — 사용자 확인 대기. 결정 시 line 126 주석/line 128 코드 일치시킬 것.

### 2026-06-25 (추가3) — 원래 파라미터 미변경 근거 + followup 분석 + 용어 정정

**질문 응답: -1.5% 가드 외 원래 전략 파라미터는 데이터로 바꾼 게 0건.** 의도된 것. followup_log(매도후 3·5·10·20일 추적)로 첫 실증 점검.
- 손절 -5%: 10건 중 7건 매도후에도 계속 하락(천일고속·동양고속·LG디플·삼성SDS d3 -12~-18%) = 좋은손절, 3건만 휩쏘(에스에이엠티 d3 +33.5%, 삼성전기 d10 +16%, SK하이닉스 d20 +39%). 느슨화하면 7건 손실 2~4배 → 유지.
- 모멘텀익절 +10%: 매도후 d3 +6.9%→d20 -7.5%. 일부 더 감(주성 d3+23, LG전자 d10+81)·일부 급락회피(LS d20-34, 현대모비스 -23). wash → 유지.
- 부분익절/모멘텀익절 검증: 레이저쎌 매도후 d20 -55% → 익절 타이밍 적중.

**용어 정정(사용자 지적).** 직전에 "BB%B·모멘텀 필터 가설과 반대 → 유지"로 한 칸에 묶은 건 부정확. 실제 kis_sector.py: 모멘텀은 하한(MIN_THEME_MOMENTUM≥15)이라 고모멘텀 추구 설계, 데이터(고모멘텀이 덜 나쁨)는 이를 '지지'(반박 아님) → analysis_plan #9의 '모멘텀 상한 추가' 불필요로 유지(일관됨). BB%B만 진짜 긴장 — (0.92~1.0]가 +5.28%지만 미래에셋생명 중복 2건=노이즈. 완화는 과열매수↑(리스크↑) 방향이라 보류.

**확립한 판단 원칙.** 보호장치(리스크↓)는 모자란 근거로도 추가, 제약 완화(리스크↑)는 노이즈로 하지 않음. 가드 신설 vs BB%B 유지의 비대칭은 이 원칙의 의도적 적용.

**열린 항목.** BB%B 0.85 페널티 과한지 = bb_pct 30건+ 후 재검증(analysis_plan에 명시). 손절 휩쏘율 30%+ 지속 시 캔들확인 강화 검토.
