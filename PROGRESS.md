# PROGRESS.md — 진행 현황

> 세션(사람/Claude)마다 작업 후 이 파일 상단에 항목을 추가한다. 최신이 위.

## 현재 상태 요약 (2026-06-12 기준)

- 단계: 모의투자 검증 진행 중 — 거래 18/30건, 누적 실현손익 +1,326,958원
- 운영: EC2 t3.micro, systemd stock-bot.service, 2주 연속 무에러
- 데이터: basis_log 13거래일 수집, 6종 CSV 정상 기록 중
- 다음 마일스톤: 거래 30건 도달 → analysis_plan.md 검증 실행

## Phase 진행 상황

| Phase | 상태 | 비고 |
|-------|------|------|
| A. 데이터 진단 분석 | 대기 | EC2 데이터 수신 대기 중 (OPERATION.md 데이터 전송 절차 참고) |
| B. 시스템 보완 | 대기 | 항목 확정됨 (TASKS.md), 착수 전 |
| C. 데이터 수집 확장 | 대기 | 빨리 반영할수록 30건 검증 가치 상승 |
| D. 전략 변경 검토 | 미도래 | 거래 30건 도달 후 |

## 기록

### 2026-06-16 (Claude 세션) — 운영 에러 분석 및 자금부족 노이즈 수정

오늘 알림 분석.
1. 09:19~09:25 롯데쇼핑(023530) "주문가능금액 부족" 🚨×3 — 새 케이스. 봇 가용현금(3.13M) > 증권사 실제 주문가능금액. 원인: `get_available_cash()`가 예수금총액(dnca_tot_amt)을 쓰는데 T+2 미결제 매도대금이 예수금엔 잡혀도 주문엔 못 씀. "매수 실패"가 긴급키워드라 🚨, 재시도해도 무의미.
   - 수정: buy_stock이 자금부족 거부를 `InsufficientFundsError`로 분리 → morning_routine에서 고가주처럼 🚨·재시도 없이 차선 후보로 전환. 테스트 검증.
2. 13:55·14:56 ⚠️ + 15:02 🚨(연속 5회 10분 전체 실패) on 000660 — 노이즈 아님. 제 per-stock 억제 덕에 단발성은 걸러지고, 실제로 오후에 모의서버(29443)가 ~10분 불안정했던 의미있는 경고. 의도대로 동작. SK하이닉스 10분 미감시였으나 결과는 +4.73%로 무사.

근본원인 수정(사용자 승인, 폴백 안전). 예수금 vs 주문가능금액 불일치(T+2) → 매수 스킵.
- kis_balance.get_orderable_cash: 매수가능조회(inquire-psbl-order, VTTC8908R) API로 미수없는 매수금액 조회.
- _execute_buy: 슬롯예산과 실제 주문가능금액 중 작은 쪽으로 수량 상한 → 거부 사전 방지, 슬롯 미사용 대신 가능한 만큼 매수.
- 조회 실패 시 None 폴백 → 기존 동작 유지(악화 없음). 격리/통합 테스트 검증.
- ⚠️ 배포 후 확인 필요: 모의서버 실제 응답 필드명(nrcvb_buy_amt 등)이 맞는지 첫 매수 때 로그/체결로 검증. 틀리면 폴백되어 무해하나 효과도 없음.

### 2026-06-15 (Claude 세션) — 당일 작업 요약

전략 로직(매수·매도 규칙) 불변. 견고성·자본효율·노이즈 개선 + 운영 정비.
- 코드 수정 4건 (배포 필요: `./scripts/deploy.sh`)
  1. 고가주 예산부족 → `InsufficientBudgetError`로 정상 스킵 (긴급알림·재시도·헛조회 제거)
  2. 네트워크 ReadTimeout/ConnectionError 3회 백오프 재시도 (kis_data)
  3. 고가주 스킵 시 차선 후보로 슬롯 대체 매수 (A안, 사용자 승인 / timing_log: bought_reserve)
  4. 종목별 연속 실패 추적으로 단발성 타임아웃 알림 억제 (연속 3회만 알림)
- 문서 정비: 손절 파라미터 정합화(-5%/-8%), 새 세션 온보딩 절차(OPERATION.md 0번 + CLAUDE.md 포인터), 운영 파일 main 반영, CLAUDE.md.txt 중복 제거 + 플러그인 설정 보존.
- 미완(사용자 작업): ① 위 코드 4건 서버 배포 ② 불필요 브랜치 3개 삭제(file-modifications-main-ykk5n, sleepy-heisenberg-8hximg, beautiful-heisenberg-wtqme4 — 샌드박스에서 삭제 푸시 불가, EC2/웹에서 실행).
- 상세는 아래 개별 기록 참고.

### 2026-06-15 (Claude 세션) — 운영 에러 2건 대응 (견고성, 전략 불변)

6/15 텔레그램 에러 2건 분석·수정.
1. 09:21 `buy_stock:009150` "예산 부족"(삼성전기 193.6만원/주, 종목당 163.7만원 → 0주). 오탐이었음 — `_execute_buy`가 generic Exception을 던져 critical 🚨 알림 + 잔고 헛조회 + 최대 3회 재시도. → `InsufficientBudgetError`로 분리해 긴급알림·재시도 없이 조용히 스킵(timing_log: skipped_unaffordable). 고가주 만날 때마다 반복되던 오탐 해결.
2. 14:06 `monitor_positions:000660` ReadTimeout(10s). `_get_with_retry`가 500/503만 재시도하고 Timeout/ConnectionError는 즉시 전파해 ⚠️ 노이즈. → Timeout/ConnectionError도 3회 백오프 재시도 추가. 격리 테스트로 검증(타임아웃 3회 후 raise / 중간 복구 / 404 즉시 raise).
   - 추가 분석(6/15 14:06·14:38·15:11 3회): 정확히 ~32분 간격 = error_monitor 30분 쿨다운(ALERT_COOLDOWN). 실제 ReadTimeout은 더 빈번하나 알림만 묶인 것. 000660만 보인 건 오후에 보유 종목이 그것뿐이어서.
   - 보강: 종목별 연속 실패 추적(`_pos_fail_streak`) 추가. 단발성 실패는 파일 기록만, 연속 3회(약 6분) 실패 종목만 알림. 조회 성공 시 리셋. 테스트 검증 완료.

배포 필요: `./scripts/deploy.sh` (코드 변경 있음).

### 2026-06-15 (Claude 세션) — 고가주 차선 후보 대체 매수 (A안, 사용자 승인)

위 한계(고가주 스킵 시 슬롯 미사용)를 바로 해결. 선정 기준·균등배분은 불변, 버려지던 검증된 차선 후보로 빈 슬롯을 채움.
- kis_sector: `reserve_count` 파라미터로 미선정 후보 반환(is_reserve 태그). 기존 호출부(telegram /signal 등)는 기본값 0이라 영향 없음.
- main: `InsufficientBudgetError` 발생 시 차선 후보를 같은 슬롯 예산(본선 종목수 고정 분모)으로 투입. 차선 소진 시 슬롯 미사용.
- timing_log 액션: 차선 매수는 `bought_reserve`, 고가 스킵은 `skipped_unaffordable` → 30건 후 차선 전환 거래 분리 분석 가능.
- 검증: kis_sector 차선 반환 단위 테스트 + morning_routine 대체 루프 통합 테스트(고가주 스킵→차선 전환→슬롯 예산 유지) 통과.
주의: 실제 매수 종목이 달라지는 변경이나 선정 로직 자체는 불변. 검증 표본에서 차선 거래는 timing_log로 구분 추적.

### 2026-06-13 (Claude 세션) — 새 세션 온보딩 절차 문서화

새 Claude 세션이 다른 브랜치에서 시작해도 서버 배포·데이터 수신이 끊기지 않도록 정비.
- OPERATION.md에 "0. 새 세션/새 브랜치 시작 시" 섹션 추가 (Claude·사용자 각각 할 일, 운영 파일 가져오는 법).
- CLAUDE.md(모든 세션이 자동으로 읽음) 상단에 OPERATION.md·PROGRESS.md 우선 정독 포인터 추가.
- 미해결: 운영 파일(OPERATION/PLAN/PROGRESS/TASKS/scripts)이 아직 main에 없음. main에서 갓 분기한 새 세션은 0-1 절차로 직전 브랜치에서 가져와야 함. main 반영 여부는 사용자 결정 대기.

### 2026-06-13 (Claude 세션) — 손절 파라미터 문서 정합화

서버 .env에는 손절 관련 키가 전혀 없음 → 손절값은 전부 config/settings.py 코드 기본값에서 옴. 코드 재확인 결과 STOP_LOSS_RATE=0.05(-5%), EMERGENCY_STOP_RATE=0.08(-8%). 초기 분석이 STOP_LOSS_RATE를 0.10으로 잘못 읽었던 것 정정.
- 결정(사용자 승인): 코드값 유지, 문서를 코드에 맞춤. 코드 변경 없음 → 재배포 불필요.
- context-notes.md 정정: 긴급손절 -15%→-8%, 일반손절 -10%→-5% (표·서술·0.85→0.92·0.90→0.95).
- checklist.md는 이미 -8%/-5%로 정확했음 (수정 불필요).
- 거래 20건 중 긴급손절(-8%) 발동 0건 — 검증 표본 영향 없음.

### 2026-06-13 (Claude 세션) — Phase B 1~3순위 구현 완료

전략 로직은 변경하지 않음. 시스템 안정성 보강 3건.
1. 유령 포지션 방지 — 매수 주문 예외 시 잔고 조회로 실제 체결 확인, 체결됐으면 신호 메타데이터 보존한 채 포지션 등록 (main.py `_verify_filled_position`). timing_log에 `bought_verified` 액션 추가됨.
2. 시세조회 마비 긴급 알림 — 모니터링 사이클에서 보유 전 종목 조회 실패가 5회(약 10분) 연속되면 텔레그램 긴급 알림, 이후 30분마다 재경보 (6/8~9 마비 재발 대비).
3. CSV 스키마 자동 정합화 — trades.csv·signal_log.csv 기록 시점에 헤더 교체+행 패딩 자동 수행 (performance.py `_ensure_csv_schema`). 실데이터 사본으로 보존·멱등성 검증 완료. 서버에서는 다음 기록 시점에 자동 적용됨.

남은 것. 서버 .env의 STOP_LOSS_RATE(-5% 추정)·EMERGENCY_STOP_RATE 값 확인 후 문서 정합화. 배포 필요 (`./scripts/deploy.sh` — 브랜치 동일하므로 인자 불필요).

### 2026-06-13 (Claude 세션) — Phase A 데이터 진단 분석 완료

성과 (포지션 19건 기준. 레이저쎌 분할매도 합산).
- 승 9 / 패 10 (승률 47%), Profit Factor 2.19, 평균익 +298,828원 / 평균손 -136,543원, 순손익 +1,622,858원
- 실전 전환 기준(승률 45%+, 손익비 1.5+)을 현재까지 충족. 단 표본 부족으로 판정은 30건 후.

핵심 발견.
1. **실제 운영 손절선은 -5%다.** 손절 10건 전부 trigger_price/entry = 0.950. [정정 2026-06-13: 손절선 -5%는 config/settings.py STOP_LOSS_RATE 코드 기본값(0.05) 그대로이며 .env 오버라이드 아님. 초기 분석이 0.10으로 잘못 읽었던 것을 코드 재확인으로 정정.] 성과상 -5%는 잘 작동 중 (손절 9건 중 7건이 매도 후 추가 하락, 반등 놓침은 에스에이엠티 +33.5% 1건).
2. **모멘텀약화 룰이 수익의 84%를 생산** (7건, 평균 +13.6%, 합계 +250만원). 이익 반납도 평균 ~2%p로 작음. 매도 후 추적에서도 대부분 추가 하락(LS d20 -34%, 현대모비스 d10 -27%) → 고점 근처 청산 성공. 예외: LG전자 매도 후 d10 +81% (대박 1건 놓침).
3. **사전 가설 기각.** "트레일 -12% 폭이 넓어 이익 반납이 클 것"은 데이터로 기각 — 트레일링 매도는 0건, 모멘텀약화가 항상 먼저 발동. 트레일 폭 축소 검토는 우선순위에서 제외.
4. **부분익절 1건(레이저쎌)은 완벽** — 매도 후 d20 -55% 폭락 전량 회피.

시스템 문제 (심각도 순).
1. **6/8~6/9 시세조회 전면 마비** — KIS 모의서버 포트 문제(9443)로 이틀간 모든 가격 조회 500 에러(366건). 보유 중이던 롯데쇼핑 모니터링이 2일간 작동 불능 → 폭락장이었다면 손절 불가였음. 포트 수정(29443)은 6/9 커밋·금일 배포로 해결됐으나, **API 장애 지속 시 긴급 알림 체계가 없다는 구조적 문제** 확인.
2. **유령 포지션** — timing_log의 buy_failed 21건 중 일부(LG전자 5/15 등)는 실제로는 체결됨. 주문 접수 후 예외 발생 → 실패로 기록·포기 → 잔고에는 존재 → 재시작 복구로 등장. 그 결과 거래 20건 중 8건이 메타데이터(진입시각·섹터·모멘텀) 유실 상태로, 신호-성과 연결 분석이 절반 무력화됨.
3. **trades.csv / signal_log.csv 스키마 드리프트** — 6/9 컬럼 추가 시 기존 파일 헤더 미갱신. 행마다 컬럼 수가 다름(signal_log: 11/15/18열 혼재) → 헤더 마이그레이션 필요.

→ Phase B 우선순위 재조정: ①주문 성공 판정 보강(유령 포지션 방지) ②API 장애 연속 감지 시 긴급 알림 ③CSV 헤더 마이그레이션 ④.env 손절값 확인 후 문서 정합화. (기존 항목 중 트레일 폭 관련 검토는 제외.)

### 2026-06-12 (Claude 세션)
- 코드베이스 전체 분석 완료. 발견 사항을 PLAN.md Phase B/C에 반영.
  - EMERGENCY_STOP_RATE 코드 기본값 0.08 vs 문서 -15% 불일치 (서버 .env 확인 필요)
  - 매수 재시도 시 중복 주문 가능성 (main.py 224~233)
  - 재시작 시 peak_price 등 트레일 상태 유실
  - 일일 손실 한도·동일 테마 제한 부재
- 운영 문서 체계 구축. PLAN / PROGRESS / TASKS / OPERATION.md 생성.
- EC2 배포가 옛 브랜치명(claude/file-modifications-main-ykk5n)에 고정된 문제 확인 → 브랜치 무관 배포 스크립트 scripts/deploy.sh 작성 (OPERATION.md 참고).
- 데이터 전송용 scripts/export_data.sh 작성 (EC2 → data-export 브랜치).
